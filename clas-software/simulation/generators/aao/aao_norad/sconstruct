from build_system import environment

aao_noradEnv = environment.env(deps='clas6 cern')
aao_noradEnv.AppendUnique(FORTRANPATH = ['.','./include'])
#aao_noradEnv.load_dependency(['clas6'])
#aao_noradEnv.load_dependency({'clas6' : ['recsis_libs', 'default_libs']})

ignore_list = ['aaosig1.F', 'ekine.F']

aao_noradEnv.program(
    target = 'aao_norad',
    ignore = ignore_list)
